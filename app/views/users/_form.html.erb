<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <%= form.text_field :first_name, autofocus: true, class: "form-control margin-bottom-md", placeholder: "first name"%>
  <%= form.text_field :last_name, class: "form-control margin-bottom-md", placeholder: "last name"%>
  <%= form.email_field :email, class: "form-control margin-bottom-md", placeholder: "email"%>

  <%= form.submit class: "btn btn-primary margin-bottom-md" %>
<% end %>

